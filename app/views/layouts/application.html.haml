!!!
%html
  %head
    %title Pair Exchange
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    - if current_user
      %div Welcome, #{current_user.name}!
      =link_to "Sign me out!", logout_path
    - else
      %div= link_to "Sign in with Github", "/auth/github"
    %div= flash[:notice]
    = yield
